// Importa a função que registra o componente raiz do Expo
import { registerRootComponent } from 'expo';

// Importa o componente principal do seu App
import App from './src/app';

// Registra o componente App como raiz do projeto
registerRootComponent(App);